Check if the service is still alive.
---
responses:
  200:
    description: "Pong"
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Service is alive"
